export const environment = {
  production: false,
  // The API's CORS policy already allows http://localhost:4200, so ng serve
  // talks to it directly — no dev proxy needed.
  apiBaseUrl: 'http://localhost:5229',
};
