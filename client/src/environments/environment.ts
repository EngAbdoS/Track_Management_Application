export const environment = {
  production: true,
  // No deployment target exists for this task; a real one would point at the
  // hosted API. Left as the local API so a production build is still runnable.
  apiBaseUrl: 'http://localhost:5229',
};
